version: "2"
authtoken: ${NGROK_AUTHTOKEN}
tunnels:
  auth:
    proto: http
    addr: auth:8000
    bind_tls: true
  chatbot:
    proto: http
    addr: chatbot:8001
    bind_tls: true
  history:
    proto: http
    addr: chatbot_history:8003
    bind_tls: true